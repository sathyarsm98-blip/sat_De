version: 2
sources:
  - name: raw
    schema: raw
    tables:
      - name: orders
        description: "Raw orders loaded via Meltano CSV."
        columns:
          - name: order_id
            tests:
              - not_null
              - unique
          - name: order_ts
            tests:
              - not_null
